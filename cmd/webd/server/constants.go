package server

const errMessageDecodeJSON = `JSON could not be decoded by the API. ` +
	`It might be invalid or you might have a type mismatch. For example, passing a string ` +
	`where an int should be.`
